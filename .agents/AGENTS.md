# Custom Rules

- ALWAYS run `git commit` to commit changes after making any source code modifications or completing a task. You can use the `run_command` tool to run `git add . ; git commit -m "..."`.
